<i id="{{ id }}" class="fa fa-question-circle ui-suggestion{% if size is defined %} fa-{{ size }}x{% endif %}" style="cursor:help"
   data-placement="n"
   data-theme="{{ theme|default('custom') }}"
   data-suggestion="{{ help_text }}"
 ></i>